# Groq for the agent LLM + structured extraction (get one at https://console.groq.com/keys)
GROQ_API_KEY=

# Composio — required for real (non-mock) research/verification.
# Create an API key with Sessions -> Write permission.
COMPOSIO_API_KEY=

# Optional: override the Groq model.
GROQ_MODEL=openai/gpt-oss-20b
